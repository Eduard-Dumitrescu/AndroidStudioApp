<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_add"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/pic4"
    tools:context="com.example.edward.androidstudiomovieapp.AddActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:id="@+id/addName"
        android:textColor="?android:attr/textColorPrimaryDisableOnly"
        android:background="@android:drawable/editbox_background"
        android:layout_alignParentTop="true"
        android:layout_alignLeft="@+id/addRating"
        android:layout_alignStart="@+id/addRating"
        android:layout_marginTop="63dp"
        android:layout_alignRight="@+id/addRating"
        android:layout_alignEnd="@+id/addRating"
        android:hint="Movie Name" />

    <RatingBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/addRating"
        android:layout_below="@+id/addName"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="70dp" />

    <Button
        android:text="Save"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/addRating"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="79dp"
        android:id="@+id/saveButton"
        android:background="@android:color/holo_blue_dark"
        android:onClick="SaveMovie"/>

</RelativeLayout>
